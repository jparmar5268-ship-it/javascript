//Q-1
//to gate user's full name and concat @ before name and concat length of name after the name

let fname = prompt("Enter Your FullName : ");
let uname = "@" + fname + fname.length;
console.log(uname);